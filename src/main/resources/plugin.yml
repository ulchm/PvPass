authors: ${project.authors}
name: ${project.name}
version: ${project.version}
depend: ${project.dependencies}
softdepend: ${project.softdependencies}
main: ${project.main}
database: true

commands:
  pvp:

permissions:
  pvpass.admin:
    default: op
    children:
      pvpass.command: true
      pvpass.command.enable: true
      pvpass.command.disable: true
      pvpass.cooldown.override: false
